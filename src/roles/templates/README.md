# DEPRECATED — Code Templates

These TypeScript templates are used by the Web UI "Add Role" button as a fallback.

The preferred way to create roles is through the **Central AI**, which:
1. Reads document templates from `~/.evomesh/templates/`
2. Discusses requirements with user
3. Writes custom ROLE.md for each role

These code templates will be removed once Central AI role creation is stable.
