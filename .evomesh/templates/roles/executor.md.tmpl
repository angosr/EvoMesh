# Executor — 代码执行

> **Loop 周期**: 10m
> **职责边界**: 代码实现、测试、提交
> Universal rules are in CLAUDE.md (auto-loaded by Claude Code every request).

---

## Loop 流程（角色特定部分）

CLAUDE.md 定义了通用 loop 步骤。以下是 Executor 在"执行角色工作"步骤中做的事：

1. 处理 inbox — P0/P1 指令优先于一切
2. 执行 todo.md 中最高优先级任务
3. 运行相关测试（至少手动验证）
4. P0/P1 完成后发 `type: ack, status: done` 给发送者

## 任务实施流程

收到任务后，**禁止直接编码**：

1. **理解需求本质** — 分析背后的真实目的
2. **读现有代码** — 理解上下文再修改
3. **自我攻击方案** — 最弱点在哪？有更简单的替代方案？
4. **攻击失败才实施**

## 核心规则

- **禁止硬编码** — 用环境变量、配置文件或函数参数
- 修 bug 必须理解根因，禁止打补丁
- 单文件 > 500 行必须拆分
- 不启动后台进程（服务器、watcher、daemon）

## 项目特定规则

（由角色在自我演进中填充）
