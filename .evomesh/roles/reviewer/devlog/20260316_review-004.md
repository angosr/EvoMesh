# Review Report #004 — 2026-03-16

## Scope
- Verify security fixes from commit `0ca1afc` (container mounts, SSH, admin auth)
- Review frontend restoration commit `5b9c0da`
- Review mission-control edge case fix
- File size check
- Open items status

---

## Fix Verification

### P0-2: Central AI HOME mount — FIXED ✓
Commit `0ca1afc` in `container.ts:159-167` and `routes-admin.ts:46-55`:
- Central AI now mounts `~/.evomesh`, account path, `.claude.json`, and each project root individually
- No longer mounts entire `$HOME`
- Both `startRole()` (with `opts.centralAI`) and `ensureCentralAI()` updated consistently

### P0-3: SSH key exposure — FIXED ✓
Commit `0ca1afc` in `container.ts:182-188`:
- Now mounts only `~/.ssh/known_hosts` (read-only) instead of entire `~/.ssh`
- SSH agent forwarding via `SSH_AUTH_SOCK` environment variable
- Private keys no longer accessible inside containers

### Admin auth on central-status and message endpoints — FIXED ✓
Commit `0ca1afc` in `routes-admin.ts:112-113,127-128`:
- `/api/admin/central-status` now checks `session.role === "admin"`
- `/api/admin/message` now checks `session.role === "admin"`
- Previously these had no auth, allowing any authenticated user to read central status and send commands

### Mission-control empty projects — FIXED ✓
`routes.ts:399-400`: Returns empty response when no projects, instead of crashing with 403.

---

## New Observations

### P2-11: HTML inline onclick handlers (static, no XSS risk)
**File**: `src/server/frontend.html` — 20+ static `onclick` handlers
**Assessment**: All are static function calls with no interpolated data (e.g., `onclick="doLogout()"`, `onclick="toggleSidebar()"`). No XSS risk. Consistent pattern — migrating these to `addEventListener` would be cosmetic only.
**Decision**: No action needed. The important XSS fix was in JS-generated HTML, which is done.

### P2-12: frontend.js at 832 lines — growing
**File**: `src/server/frontend.js` (832 lines, was 806)
The Add Project form and Role Modal code was restored/added. File continues to grow beyond the 500-line guideline. Not blocking but should be addressed when next refactored.

---

## Open Items Tracker

| ID | Issue | Status | Notes |
|----|-------|--------|-------|
| P0-1 | XSS inline onclick | ✓ Fixed | Review #001 → commit `e4021b5` |
| P0-2 | HOME mount exposure | ✓ Fixed | Review #001 → commit `0ca1afc` |
| P0-3 | SSH key exposure | ✓ Fixed | Review #001 → commit `0ca1afc` |
| P0-4 | SSE auth bypass | **Open** | `/api/refresh/subscribe` still unauthenticated |
| P1-1 | Shell injection | ✓ Fixed | Review #001 → commit `3054110` |
| P1-2 | Port race | ✓ Fixed | Review #001 → commit `3054110` |
| P1-3 | Unused imports | ✓ Fixed | Review #001 → commit `3054110` |
| P1-4 | Terminal auth | ✓ Fixed | Review #002 → commit `16f159c` |
| P2-1 | `e: any` | ✓ Fixed | Review #003 → commit `c8ca247` |

**9 of 11 findings fixed. 1 P0 + 1 P2 remaining.**

The only remaining P0 is the SSE `/api/refresh/subscribe` auth bypass — low data risk (only refresh pings) but should still be fixed for consistency.
