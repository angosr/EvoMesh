# Review Report #006 — 2026-03-16

## Scope
- Verify P2 fixes: timing-safe auth, Dockerfile pins, unused dependency
- Review mission-control API enhancements
- File size check

---

## Fix Verification

### SEC-010: Timing side-channel in password comparison — FIXED ✓
**File**: `src/server/auth.ts:113,122`
Both `verifyUser()` and `changePassword()` now use `crypto.timingSafeEqual(Buffer.from(hash, "hex"), Buffer.from(user.passwordHash, "hex"))`. Correct — both buffers are same length (PBKDF2 64-byte output → 128 hex chars).

### SEC-011: Unpinned Dockerfile dependency — FIXED ✓
**File**: `docker/Dockerfile:10`
Changed from `@anthropic-ai/claude-code@latest` to `@anthropic-ai/claude-code@2.1.76`. Pinned version.

### P2-6: Unused Python/websockets dependency — FIXED ✓
**File**: `docker/Dockerfile`
Removed `RUN pip3 install --break-system-packages websockets`. Dead dependency cleaned up.

Note: `python3` and `python3-pip` are still installed in the apt line but `pip3` is no longer used. Could remove `python3-pip` from apt install. Minor.

---

## Mission Control API Enhancement — CLEAN ✓
**File**: `src/server/routes.ts:398-475`

Good improvements:
- `alerts` renamed to `issues` with typed `type` field (`stale`, `stopped`, `p0-pending`)
- Added stale detection: flags roles with short-term memory >1 hour old
- Added `time` field with relative timestamps (`relTime()`)
- Activity sorted by mtime (most recent first)
- Tasks now exclude completed items (reduces payload)
- Unused `rc` variable destructured away (`[name]` instead of `[name, rc]`)

No issues found.

---

## File Size

| File | Lines | Status |
|------|-------|--------|
| `routes.ts` | 485 | ⚠️ 97% of 500 limit — 15 lines remaining |

**Action needed**: One more feature addition will breach the limit. The mission-control block (lines 398-475) is a natural extraction candidate → `routes-mission.ts`.

---

## Open Items Status

| ID | Issue | Status |
|----|-------|--------|
| P0-4 | SSE `/api/refresh/subscribe` no auth | Open (low risk) |
| P2 | `frontend.js` 823 lines | Open (guideline, not hard rule) |
| P2 | `routes.ts` 485/500 lines | ⚠️ Critical — extract mission-control soon |
| P2 | `python3-pip` still in Dockerfile apt | Minor |

**All P0 security and P1 bug findings are resolved.**
