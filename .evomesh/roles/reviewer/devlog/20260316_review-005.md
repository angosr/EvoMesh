# Review Report #005 — 2026-03-16

## Scope
- Review frontend cleanup (removed Add Project/Role Modal)
- Review new `simpleMarkdown()` function for XSS
- Review terminal token passthrough in iframe
- Cross-check with security role audit #002
- File size tracking

---

## Changes Reviewed

### Frontend cleanup (commit `31f8e8c`) — CLEAN ✓
Add Project form and Role Modal removed from both HTML and JS per user directive. Dead code properly removed. Frontend.js dropped from 832 to 823 lines (net -9). No orphaned references found.

### `simpleMarkdown()` function — SAFE ✓
**File**: `src/server/frontend.js:740-756`
Escapes `&` and `<` before applying markdown transforms. Output used via `innerHTML` but input is pre-escaped. No XSS vector.

### Terminal auth token in iframe URL — CORRECT ✓
**File**: `src/server/frontend-panels.js:12`
```js
const authPath = terminalPath + (terminalPath.includes('?') ? '&' : '?') + 'token=' + encodeURIComponent(AUTH_TOKEN);
```
Token is properly URI-encoded and appended to the terminal iframe src. This allows `terminal.ts` to extract and set the auth cookie on first load. Correct integration with the cookie-based terminal auth from commit `16f159c`.

### Security audit #002 cross-check — ALIGNED ✓
Security role independently verified the same 3 P0 fixes (SEC-001/002/003). Found 3 additional P2 items:
- SEC-010: Timing side-channel in password comparison (academic risk)
- SEC-011: Unpinned npm dependency in Dockerfile (supply chain)
- SEC-012: `--dangerously-skip-permissions` (accepted architectural risk)

All align with reviewer findings. No contradictions.

---

## File Size Status

| File | Lines | Limit | Status |
|------|-------|-------|--------|
| `routes.ts` | 475 | 500 | ⚠️ 95% |
| `frontend.js` | 823 | 500* | ⚠️ Over (JS, not TS rule) |
| All other TS files | <410 | 500 | ✓ |

*500-line rule is for TypeScript files per ROLE.md. Frontend JS is guideline, not hard rule.

---

## Conclusion

**No new findings this cycle.** Codebase is in good shape:
- All critical security issues (P0) are resolved
- Code quality has improved significantly (typed errors, auth, scoped mounts)
- Frontend is cleaner after dead code removal
- Security role providing independent verification

Remaining low-priority items: SSE auth bypass (P0-4), file size warnings, Dockerfile pins.
