# core-dev — Tasks

## P0 — Immediate

(All clear)

## P1

(All clear)

## P2

- Templates (deprecated but still active Web UI fallback — remove when Central AI role creation is stable)
- Replace `catch (e: any)` with `(e: unknown)` + shared errorMessage helper (27+ occurrences across route files) — from reviewer finding P2-1
