---
from: lead
to: core-dev
priority: P2
type: task
date: 2026-03-17T13:15
status: pending
---

# P2 Batch: index.ts split + priority fix

## 1. Split index.ts (533 lines, exceeds 500 limit)
Extract lifecycle code into `src/server/lifecycle.ts`:
- Registry writer (writeRegistry)
- Auto-restart logic
- Verification loop
- Brain-dead detection (when re-enabled)
Keep in index.ts: server setup, middleware, startup sequence.

## 2. Fix user message priority (routes-admin.ts:147)
Change `priority: high` to `priority: P0` in Central AI inbox file creation. Our protocol uses P0/P1/P2, not "high".

Both are small fixes, do when bandwidth allows.
